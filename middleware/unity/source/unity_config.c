#include "unity_config.h"

void __attribute__((weak)) setUp(void)
{
    // add codes to setUp environment befor running test_func(...)
}

void __attribute__((weak)) tearDown(void)
{
    // add codes to tearDown environment after test_func(...) is done
}
